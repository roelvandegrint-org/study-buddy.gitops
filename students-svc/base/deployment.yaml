apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: students-svc
  name: students-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: students-svc
  template:
    metadata:
      labels:
        app: students-svc
    spec:
      containers:
        - name: roelvandegrint.azurecr.io/study-buddy
          image: roelvandegrint/image:0.0.1
